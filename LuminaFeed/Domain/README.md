# Domain

Domain entities, enums, and value objects (`Category`, `Feed`, `Subscription`, `Article`, and
their supporting types). Plain POCOs, persisted via EF Core (`Data/`). All new entities use
**GUID v7** IDs (see `CLAUDE.md`).
